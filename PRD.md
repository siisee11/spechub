SpecHub is the open community(marketplace) that people can share their Idea as a spec file.

Use this repo as a database.

All specs are stored in specs/ folder as subfolder. (ex. specs/create-harness/...)

Make website similar to https://skills.sh/

Provide download spec button to copy link to command that download subfolder from github and extract to pwd. (ex. curl ....../install.sh & sh install.sh)

# Frontend

The worktree preview server exists so agents can inspect rendered repository content with `agent-browser`. Keep the UI intentionally simple and deterministic.

The product website now lives in `apps/web` (Vite + React). Use `apps/web/README.md` for local development, build, and Wrangler deployment workflow.
